@model IEnumerable<OneToOne.TblGuardian>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.G_no)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.G_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TblStudent.Std_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.G_no)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.G_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TblStudent.Std_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.R_no }) |
            @Html.ActionLink("Details", "Details", new { id=item.R_no }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.R_no })
        </td>
    </tr>
}

</table>
